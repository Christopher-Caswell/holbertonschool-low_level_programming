#include "holberton.h"

/**
 * _puts - print a string
 * @str: the string
 * Return: nada
 */

void _puts(char *str)
{

char x;
x = str[0];

while (x >= 0 && x != '\0')
{

_putchar(x);
 x++;
}

_putchar('\n');


return;
}
